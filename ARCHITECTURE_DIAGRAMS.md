# 🏗️ E-GovToba Producer-Consumer Architecture Visual Guide

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      END USER (Browser)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Request
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│             CONSUMER APP (Main Application)                     │
│             Port: 8000                                          │
│             http://localhost:8000                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Web Routes (routes/web.php)                             │  │
│  │  - GET /districts                                        │  │
│  │  - GET /villages                                         │  │
│  │  - GET /announcements                                    │  │
│  │  - etc.                                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Controllers (app/Http/Controllers/)                    │  │
│  │  - DistrictController                                   │  │
│  │  - VillageController                                    │  │
│  │  - AnnouncementController                               │  │
│  │                                                          │  │
│  │  Dependency Inject ApiService                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ApiService (app/Services/ApiService.php)               │  │
│  │                                                          │  │
│  │  Methods:                                                │  │
│  │  - getDistricts()                                        │  │
│  │  - getVillages()                                         │  │
│  │  - getAnnouncements()                                    │  │
│  │  - searchDistricts()                                     │  │
│  │  - ... (more methods)                                    │  │
│  │                                                          │  │
│  │  Features:                                               │  │
│  │  ✅ Automatic Caching (Redis/File)                       │  │
│  │  ✅ Error Handling                                       │  │
│  │  ✅ Timeout Protection                                   │  │
│  │  ✅ Retry Logic (optional)                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          │ HTTP GET Request                     │
│                          │ (via Illuminate\Support\Facades\Http) │
│                          ▼                                      │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│             PRODUCER API (API Server)                           │
│             Port: 8001                                          │
│             http://localhost:8001                               │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API Routes (producer-api/routes/api.php)                │  │
│  │  - GET /api/v1/districts                                 │  │
│  │  - GET /api/v1/districts/{id}                            │  │
│  │  - GET /api/v1/villages                                  │  │
│  │  - GET /api/v1/villages/{id}                             │  │
│  │  - GET /api/v1/announcements                             │  │
│  │  - GET /api/v1/announcements/{id}                        │  │
│  │  - GET /api/v1/districts/{id}/villages                   │  │
│  │  - GET /api/v1/villages/{id}/announcements               │  │
│  │  - GET /api/v1/health (status)                           │  │
│  │  - GET /api/v1/*/search?q=query                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Api Controllers (producer-api/app/Http/Controllers/Api/)│  │
│  │  - DistrictController                                    │  │
│  │  - VillageController                                     │  │
│  │  - AnnouncementController                                │  │
│  │  - HealthController                                      │  │
│  │                                                          │  │
│  │  Methods:                                                │  │
│  │  - index()      → GET all                                │  │
│  │  - show($id)    → GET by ID                              │  │
│  │  - search()     → Search by query                        │  │
│  │  - byXxx()      → Get by relationship                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          │ Query                                │
│                          ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Models (producer-api/app/Models/)                       │  │
│  │  - District.php (with relationships)                     │  │
│  │  - Village.php (with relationships)                      │  │
│  │  - Announcement.php (with relationships)                 │  │
│  │  - User.php (with Sanctum tokens)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          │                                      │
│                          │ ORM Query                            │
│                          ▼                                      │
└─────────────────────────────────────────────────────────────────┘
                             │
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│                    SHARED DATABASE                              │
│                    (e_govtoba)                                  │
│                                                                 │
│  Tables:                                                        │
│  - districts                                                    │
│  - villages                                                     │
│  - announcements                                                │
│  - users                                                        │
│  - citizens                                                     │
│  - services                                                     │
│  - service_requests                                             │
│  - etc.                                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────┐
│ 1. User Request                                              │
│    GET http://localhost:8000/districts                       │
└────────────────────────┬─────────────────────────────────────┘
                         │
         ┌───────────────┴────────────────┐
         │                                │
         ▼                                ▼
    ┌─────────────────────────────────────────────────┐
    │ 2. Router Match                                 │
    │    web.php: Route::get('/districts', [..])      │
    └──────────────┬────────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
         ▼                    ▼
    ┌─────────────────────────────────────────────────┐
    │ 3. Call Controller                              │
    │    DistrictController@index()                   │
    └──────────────┬────────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
         ▼                    ▼
    ┌─────────────────────────────────────────────────┐
    │ 4. Inject ApiService via DI                     │
    │    public function __construct(ApiService $api) │
    └──────────────┬────────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
         ▼                    ▼
    ┌─────────────────────────────────────────────────┐
    │ 5. Check Cache                                  │
    │    Cache::has('api_districts_all')              │
    └──────────────┬────────────────────────────────────┘
                   │
        ┌──────────┴────────────┐
        │                       │
   [CACHED]             [NOT CACHED]
        │                       │
        ▼                       ▼
   ┌────────────┐    ┌──────────────────────────────┐
   │ Return     │    │ 6. HTTP Request to Producer  │
   │ From Cache │    │    Http::get(...8001/api...) │
   └─────┬──────┘    └──────────┬───────────────────┘
         │                      │
         │          ┌───────────┴──────────┐
         │          │                      │
         │          ▼                      ▼
         │    ┌────────────────────────────────────┐
         │    │ 7. Producer Route Match            │
         │    │    api.php: Route::get(...)        │
         │    └─────────┬──────────────────────────┘
         │              │
         │    ┌─────────┴──────────┐
         │    │                    │
         │    ▼                    ▼
         │ ┌──────────────────────────────────────┐
         │ │ 8. Call Api Controller               │
         │ │    DistrictController@index()        │
         │ └─────────┬──────────────────────────────┘
         │           │
         │ ┌─────────┴──────────┐
         │ │                    │
         │ ▼                    ▼
         │ ┌──────────────────────────────────────┐
         │ │ 9. Query Database via Model          │
         │ │    District::with('villages')->get() │
         │ └─────────┬──────────────────────────────┘
         │           │
         │ ┌─────────┴──────────┐
         │ │                    │
         │ ▼                    ▼
         │ ┌──────────────────────────────────────┐
         │ │ 10. Return JSON Response             │
         │ │     {status, message, data, count}   │
         │ └─────────┬──────────────────────────────┘
         │           │
         └───────────┴───────────┐
                                 │
                    ┌────────────┴───────────┐
                    │                        │
                    ▼                        ▼
         ┌──────────────────────────────────────────────┐
         │ 11. ApiService Receive Response              │
         │     if (response->successful())              │
         │         Cache::put(...)                      │
         │         return response                      │
         └─────────┬────────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
         ▼                    ▼
    ┌──────────────────────────────────────────────────┐
    │ 12. Controller Extract Data                      │
    │     $districts = $response['data']               │
    └─────────┬─────────────────────────────────────────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
    ┌──────────────────────────────────────────────────┐
    │ 13. Pass to View                                 │
    │     return view('districts', compact(...))       │
    └─────────┬─────────────────────────────────────────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
    ┌──────────────────────────────────────────────────┐
    │ 14. Render Blade Template                        │
    │     @foreach ($districts as $district)           │
    │         {{ $district['name'] }}                  │
    │     @endforeach                                  │
    └─────────┬─────────────────────────────────────────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
    ┌──────────────────────────────────────────────────┐
    │ 15. Return HTML Response                         │
    │     HTTP 200 + HTML                              │
    └─────────┬─────────────────────────────────────────┘
              │
    ┌─────────┴──────────┐
    │                    │
    ▼                    ▼
    ┌──────────────────────────────────────────────────┐
    │ 16. Browser Render                               │
    │     Display HTML to User                         │
    └──────────────────────────────────────────────────┘
```

---

## File Relationship Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      routes/web.php                             │
│            (Consumer Web Routes Configuration)                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ defines
                         │
                 ┌───────┴────────┐
                 │                │
                 ▼                ▼
        ┌──────────────────────────────────┐
        │ DistrictController               │
        │ (Consumer - Main App)             │
        │                                  │
        │ public function index()          │
        │   ApiService $api                │
        │   └─ $api->getDistricts()        │
        └──────────────┬───────────────────┘
                       │
                       │ uses
                       │
                       ▼
        ┌──────────────────────────────────┐
        │ ApiService                       │
        │ (app/Services/ApiService.php)    │
        │                                  │
        │ public function getDistricts()   │
        │   Http::get(API_BASE_URL)        │
        │   Cache::put(...)                │
        │   return $response               │
        └──────────────┬───────────────────┘
                       │
                       │ makes HTTP request to
                       │
                 ┌─────┴─────────────────────┐
                 │                           │
     API_BASE_URL=http://localhost:8001/api/v1
                 │                           │
                 ▼                           │
    ┌──────────────────────────────────┐    │
    │ routes/api.php                   │◄───┘
    │ (Producer API Routes)             │
    │ GET /v1/districts                │
    │ GET /v1/villages                 │
    │ GET /v1/announcements            │
    │ etc.                             │
    └──────────────┬───────────────────┘
                   │
                   │ defines endpoints
                   │
    ┌──────────────┴────────────────────┐
    │                                   │
    ▼                                   ▼
┌──────────────────────────────┐  ┌──────────────────────────────┐
│ Api/DistrictController       │  │ Api/VillageController        │
│ (Producer - API Server)      │  │ (Producer - API Server)      │
│                              │  │                              │
│ public function index()      │  │ public function index()      │
│   District::with()->get()    │  │   Village::with()->get()     │
│   return json(...)           │  │   return json(...)           │
└──────────────┬───────────────┘  └──────────────┬───────────────┘
               │                                 │
               │ queries                         │ queries
               │                                 │
       ┌───────┴─────────┐           ┌───────────┴────────┐
       │                 │           │                    │
       ▼                 ▼           ▼                    ▼
┌────────────────┐  ┌────────────────────────┐  ┌────────────────┐
│ District Model │  │ Village Model          │  │ Announcement   │
│ (Producer)     │  │ (Producer)             │  │ Model          │
│                │  │                        │  │ (Producer)     │
│ - id           │  │ - id                   │  │                │
│ - name         │  │ - name                 │  │ - id           │
│ - code         │  │ - district_id          │  │ - title        │
│ - etc.         │  │ - etc.                 │  │ - content      │
└────────────────┘  └────────────────────────┘  └────────────────┘
       │                     │                          │
       │ maps to database    │ maps to database         │ maps to
       │ table              │ table                    │ database table
       ▼                     ▼                          ▼
    ┌──────────────────────────────────────────────────────────┐
    │                    E-GovToba Database                    │
    │                   (Shared by Both)                        │
    │                                                          │
    │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐    │
    │  │ districts   │  │ villages     │  │ announcements│    │
    │  │             │  │              │  │              │    │
    │  │ id          │  │ id           │  │ id           │    │
    │  │ name        │  │ name         │  │ title        │    │
    │  │ code        │  │ district_id  │  │ content      │    │
    │  │ etc.        │  │ etc.         │  │ etc.         │    │
    │  └─────────────┘  └──────────────┘  └──────────────┘    │
    │                                                          │
    └──────────────────────────────────────────────────────────┘
```

---

## Request/Response Flow

### Scenario: User meminta daftar district

```
1. BROWSER REQUEST
   ├─ GET http://localhost:8000/districts
   └─ No auth required (public access)

2. CONSUMER ROUTER
   ├─ Match: Route::get('/districts', [DistrictController::class, 'index'])
   └─ Load: DistrictController

3. CONSUMER CONTROLLER
   ├─ Inject: ApiService
   ├─ Call: $api->getDistricts()
   └─ Check cache first

4. CACHE CHECK
   ├─ If cached → Return from cache
   └─ If not → Continue to next step

5. API SERVICE
   ├─ Build request: Http::get('http://localhost:8001/api/v1/districts')
   ├─ Set timeout: 30 seconds
   └─ Send HTTP request

6. PRODUCER ROUTE
   ├─ Match: Route::get('/api/v1/districts', [...])
   └─ Load: Api/DistrictController

7. PRODUCER CONTROLLER
   ├─ Query: District::with('villages')->get()
   ├─ Format: JSON response
   ├─ Include: status, message, data, count
   └─ Return: HTTP 200

8. PRODUCER DATABASE
   ├─ Execute: SELECT * FROM districts JOIN villages
   ├─ Return: Result set
   └─ Format: Eloquent collection

9. API SERVICE
   ├─ Receive: JSON response
   ├─ Check: response->successful()
   ├─ Cache: Cache::put(..., 60 minutes)
   └─ Return: Array data

10. CONSUMER CONTROLLER
    ├─ Extract: $districts = $response['data']
    ├─ Prepare: compact('districts')
    └─ Call: view('districts', ...)

11. BLADE TEMPLATE
    ├─ Loop: @foreach ($districts as $district)
    ├─ Display: {{ $district['name'] }}
    └─ Render: HTML output

12. BROWSER
    ├─ Receive: HTTP 200 + HTML
    ├─ Parse: HTML/CSS/JS
    └─ Display: Web page to user
```

---

## Performance Timeline

```
Timeline (milliseconds)

0ms    ┌─ User clicks link
       │
10ms   ├─ Browser sends HTTP request
       │
15ms   ├─ Consumer router matches
       │
20ms   ├─ Controller loaded
       │
25ms   ├─ ApiService injected
       │
30ms   ├─ Check cache...
       │
       ├─ [IF CACHED]
       │  └─ 50ms  ├─ Return from cache ✅ FAST
       │
       ├─ [IF NOT CACHED]
       │  ├─ 35ms  ├─ Build HTTP request
       │  │
       │  ├─ 50ms  ├─ Send to Producer (localhost 8001)
       │  │
       │  ├─ 55ms  ├─ Producer route match
       │  │
       │  ├─ 60ms  ├─ Database query
       │  │
       │  ├─ 100ms ├─ Format JSON response
       │  │
       │  ├─ 105ms ├─ Return to Consumer
       │  │
       │  ├─ 110ms ├─ Cache result
       │  │
       │  └─ 115ms ├─ Return from ApiService
       │
       ├─ 120ms ├─ Extract data
       │
       ├─ 125ms ├─ Render template
       │
       ├─ 200ms ├─ Browser render HTML
       │
       └─ 200+ms └─ Display to user

Average Response Time:
- From Cache: 50ms ⚡ FAST
- First Request: 120-200ms (acceptable)
```

---

**Visualisasi lengkap arsitektur E-GovToba Producer-Consumer telah dibuat!**
